# Next Build Steps (Roadmap)

## This Week
1. Confirm Supabase schema + RLS policies are committed to `backend/schema.sql`
2. Seed demo institution + demo ombuds user
3. Build Desktop MVP (web) inbox:
   - New / Active / Resolved columns
   - Thread view
   - Reply box
   - Mark resolved

## Next
4. Build Mobile MVP (native):
   - Institution select
   - Role select
   - Free-text message submit
   - Thread view + push notifications (optional)
   - Recovery key flow

## Later
5. Archive view (v2)
6. Attachments (v2+)
7. Client-side encryption upgrade (v2+)
